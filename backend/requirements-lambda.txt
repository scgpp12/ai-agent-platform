# Lambda 同梱用（uvicorn/pytest/boto3 は除く）
# - uvicorn 不要（Mangum が ASGI を担う）
# - boto3 不要（Lambda ランタイムに同梱済み）
# - cryptography / pydantic-core は C/Rust 拡張 → manylinux wheel を取得する必要あり
#   （build_lambda.sh が --platform manylinux2014_x86_64 で取得する）
fastapi==0.115.6
mangum==0.19.0
PyJWT==2.10.1
cryptography==44.0.0
httpx==0.28.1
